# Release 1.0.1 🖥️

### Added
- **Branding**: Implemented custom Proxmox Menu Bar Icon (replacing generic server symbol).
- **Network**: Enabled `NSAllowsArbitraryLoads` to resolve "TLS Error" for users with strict self-signed certs.

### Fixed
- **UI**: Removed redundant divider after the last item in the resource list.
